const createDirectoryIfNotExisted = require('./create-directory-if-not-existed');
const deleteFiles = require('./delete-files');
const writeFile = require('./write-file');

module.exports = {
	createDirectoryIfNotExisted,
	deleteFiles,
	writeFile,
};
